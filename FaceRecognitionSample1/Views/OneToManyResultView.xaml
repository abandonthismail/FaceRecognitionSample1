<UserControl x:Class="FaceRecognitionSample1.Views.OneToManyResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.expression/blend/2008" 
             xmlns:properties="clr-namespace:FaceRecognitionSample1.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.ResultTitleOneToMany}" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="130" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock Text="{x:Static properties:Resources.QueryImage}" HorizontalAlignment="Center" Margin="0,0,0,8" Foreground="Gray"/>
                <Border Width="110" Height="110" Background="#F0F2F5" CornerRadius="6" BorderBrush="LightGray" BorderThickness="1">
                    <Image Source="{Binding QueryImage}" Stretch="UniformToFill"/>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="{x:Static properties:Resources.TopCandidateImage}" HorizontalAlignment="Center" Margin="0,0,0,8" Foreground="Gray"/>
                <Border Width="110" Height="110" Background="#F0F2F5" CornerRadius="6" BorderBrush="LightGray" BorderThickness="1">
                    <Image Source="{Binding TopCandidateImage}" Stretch="UniformToFill"/>
                </Border>
            </StackPanel>

            <Border Grid.Column="2" Background="#F8F9FA" CornerRadius="8" Padding="10" VerticalAlignment="Top" Margin="5,22,5,0">
                <StackPanel HorizontalAlignment="Stretch">
                    <TextBlock Text="{x:Static properties:Resources.ScoreLabel}" FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Score, TargetNullValue='-', StringFormat=\{0:F4\}}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,12"/>
                    <Border Height="1" Background="#E0E0E0" Margin="0,0,0,10"/>
                    <TextBlock Text="{x:Static properties:Resources.JudgmentLabel}" FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
                    <TextBlock Text="-" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Gray"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMatch}" Value="True">
                                        <Setter Property="Foreground" Value="#28A745"/>
                                        <Setter Property="Text" Value="{x:Static properties:Resources.StatusMatch}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsMatch}" Value="False">
                                        <Setter Property="Foreground" Value="#DC3545"/>
                                        <Setter Property="Text" Value="{x:Static properties:Resources.StatusMismatch}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding SubCandidates}" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="6,0,6,0" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding RankText}" FontSize="12" Foreground="DimGray" HorizontalAlignment="Left" Margin="2,0,0,4"/>
                        
                        <Border Width="75" Height="75" Background="#F0F2F5" CornerRadius="4" BorderBrush="LightGray" BorderThickness="1">
                            <Image Source="{Binding Image}" Stretch="UniformToFill"/>
                        </Border>
                        
                        <TextBlock Text="{Binding Score, StringFormat=\{0:F4\}}" FontSize="11" FontWeight="SemiBold" Foreground="#28A745" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>