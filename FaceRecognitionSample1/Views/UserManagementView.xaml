<UserControl x:Class="FaceRecognitionSample1.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FaceRecognitionSample1.ViewModels"
             xmlns:properties="clr-namespace:FaceRecognitionSample1.Properties"
             mc:Ignorable="d" 
             d:DesignWidth="1200" d:DesignHeight="800" Background="White">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernSpinnerStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Canvas Width="40" Height="40">
                            <Path Data="M 20 2 A 18 18 0 1 1 2 20" Stroke="#0052CC" StrokeThickness="4" StrokeEndLineCap="Round" StrokeStartLineCap="Round" Fill="Transparent"/>
                            <Canvas.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotation" CenterX="20" CenterY="20" />
                            </Canvas.RenderTransform>
                            <Canvas.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SpinnerRotation" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Canvas.Triggers>
                        </Canvas>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CenterTextColumnStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.UserManagementTitle}" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20" VerticalAlignment="Center"/>

        <Border Grid.Row="1" Background="#F9FAFB" CornerRadius="8" Padding="24" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="{x:Static properties:Resources.UserSearchLabel}" Foreground="#666666" FontSize="12" Margin="0,0,0,6"/>
                    <Border Width="320" Height="40" CornerRadius="6" BorderBrush="#D1D5DB" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" Foreground="#9CA3AF" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBox Grid.Column="1" BorderThickness="0" Background="Transparent" Text="{Binding SearchText}"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="{x:Static properties:Resources.RegistrationDatePeriodLabel}" Foreground="#666666" FontSize="12" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal">
                        <DatePicker SelectedDate="{Binding StartDate}" Width="140" Height="36" VerticalContentAlignment="Center" BorderBrush="#DDDDDD"/>
                        <TextBlock Text="{x:Static properties:Resources.DateRangeSeparator}" VerticalAlignment="Center" Margin="10,0"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Width="140" Height="36" VerticalContentAlignment="Center" BorderBrush="#DDDDDD"/>
                    </StackPanel>
                </StackPanel>

                <Button Content="{x:Static properties:Resources.SearchButton}" Command="{Binding SearchCommand}" Width="80" Height="44" Margin="10,20,0,0" Background="#EAEAEA" BorderThickness="0" Cursor="Hand" FontWeight="Medium"/>
                <Button Content="{x:Static properties:Resources.ClearSearchButton}" Command="{Binding ClearSearchCommand}" Width="90" Height="44" Margin="10,20,0,0" Background="Transparent" BorderThickness="0" Foreground="#0052CC" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False"
                  Background="Transparent" BorderThickness="0" 
                  GridLinesVisibility="None" RowHeight="54" IsReadOnly="True" Margin="0,0,0,16"
                  AlternatingRowBackground="#FDFDFD">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Height" Value="50"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="Padding" Value="10,0"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridColumnHeader">
                                    <Grid>
                                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"/>

                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Margin="{TemplateBinding Padding}"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E5E7EB"/>
                                            <Setter Property="Foreground" Value="#0052CC"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#D1D5DB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F5FF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="50">
                        <DataGridTemplateColumn.HeaderTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding DataContext.IsAllSelected, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.HeaderTemplate>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="{x:Static properties:Resources.FaceThumbnailColumn}" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Transparent" ClipToBounds="False">
                                    <Ellipse Width="42" Height="42">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="{Binding FaceThumbnail}" Stretch="UniformToFill"/>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="{x:Static properties:Resources.UserIdColumn}" Binding="{Binding UserId}" Width="150" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTextColumn Header="{x:Static properties:Resources.UserNameColumn}" Binding="{Binding UserName}" Width="*" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    <DataGridTextColumn Header="{x:Static properties:Resources.RegistrationDateColumn}" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy/MM/dd}}" Width="150" ElementStyle="{StaticResource CenterTextColumnStyle}"/>

                    <DataGridTemplateColumn Header="{x:Static properties:Resources.OperationColumn}" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Hyperlink Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                           CommandParameter="{Binding}" Foreground="#0052CC" TextDecorations="None">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            FontFamily="Segoe MDL2 Assets"
                                            Text="&#xE70F;"
                                            Foreground="#2563EB"
                                            Margin="0,0,4,0"/>
                                        <TextBlock
                                            Text="{x:Static properties:Resources.EditButton}"
                                            Foreground="#2563EB"/>
                                    </StackPanel>
                                </Hyperlink>
                            </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{Binding DeleteButtonText}" Command="{Binding DeleteSelectedUsersCommand}"
                    HorizontalAlignment="Left" Width="160" Height="44" Padding="20,0" FontWeight="Bold" BorderThickness="1" Cursor="Hand" 
                    Background="White" BorderBrush="#FF4D4F" Foreground="#FF4D4F">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6"
                                BorderBrush="#EF4444"
                                BorderThickness="1"
                                Background="White">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="&lt;" Width="36" Height="44" Margin="0,0,5,0" Background="#F5F5F5" BorderThickness="0" Cursor="Hand"/>
                <Button Content="1" Width="36" Height="44" Margin="0,0,5,0" Background="#0052CC" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                <Button Content="&gt;" Width="36" Height="44" Margin="0,0,20,0" Background="#F5F5F5" BorderThickness="0" Cursor="Hand"/>

                <Button Content="{x:Static properties:Resources.BackButtonText}" Command="{Binding GoBackCommand}"
                        Width="160" Height="44" Margin="0,0,10,0" Background="#F5F5F5" BorderThickness="0" Cursor="Hand"/>
                <Button Content="{x:Static properties:Resources.CreateUserButton}" Command="{Binding CreateUserCommand}"
                        Width="160" Height="44" Background="#0052CC" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <Grid Background="#B2FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <Control Style="{StaticResource ModernSpinnerStyle}" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>